{
  "framework": "nextjs",
  "buildCommand": "prisma generate && prisma db push && next build",
  "installCommand": "npm install",
  "regions": ["cdg1"],
  "env": {
    "NEXT_PUBLIC_APP_NAME": "Dagan IA"
  },
  "crons": [
    {
      "path": "/api/cron/rapports",
      "schedule": "0 7 * * *"
    }
  ]
}
